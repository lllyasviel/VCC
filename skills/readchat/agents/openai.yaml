interface:
  display_name: "VCC Read Chat"
  short_description: "VCC: Review one known agent session from its JSONL log"
  default_prompt: "Use $readchat to inspect this session log and recover the relevant details."
