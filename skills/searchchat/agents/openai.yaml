interface:
  display_name: "VCC Search Chat"
  short_description: "VCC: Search local history across supported agent clients"
  default_prompt: "Use $searchchat to find prior sessions about this topic and show the strongest evidence."
